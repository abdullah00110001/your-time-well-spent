/**
 * App version constants.
 *
 * Keep these values in sync with android/app/build.gradle. The installed 1.0.5
 * APK used versionCode 3473498, so 3473499 is the smallest valid update code.
 * CI is intentionally not allowed to override either value.
 */
export const CURRENT_APP_VERSION_CODE = 3473499;
export const CURRENT_APP_VERSION_NAME = '1.0.21';
