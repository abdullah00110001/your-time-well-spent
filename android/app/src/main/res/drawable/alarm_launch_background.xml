<?xml version="1.0" encoding="utf-8"?>
<!-- Dark/warm alarm launch background. Used by AppTheme.AlarmLaunch so the
     full-screen alarm never shows the white cold-start splash. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:angle="270"
                android:startColor="@color/alarm_bg_top"
                android:endColor="@color/alarm_bg_bottom"
                android:type="linear" />
        </shape>
    </item>
</layer-list>
